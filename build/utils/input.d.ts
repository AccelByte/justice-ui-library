/**
 * find the input array have "e" letter, plus, dot, comma, and dash symbol
 * @param inputArray
 * @param minimumValue
 * @param isFloat
 */
export declare function isForbiddenKey(key: string, minimumValue?: number | string, isFloat?: boolean): boolean;
